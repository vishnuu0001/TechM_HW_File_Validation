{
  "name": "excel-validator-app",
  "version": "1.0.0",
  "description": "Excel validation tool with React frontend and Flask backend",
  "scripts": {
    "build": "npm --prefix frontend run build",
    "start": "npm --prefix frontend start",
    "build:vercel": "npm --prefix frontend ci && npm --prefix frontend run build && rm -rf build && cp -R frontend/build ./build"
  },
  "keywords": [
    "excel",
    "validation",
    "react",
    "flask"
  ],
  "author": "",
  "license": "ISC"
}
